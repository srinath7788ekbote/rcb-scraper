# ── Mode ──────────────────────────────────────────────────────────────────────
# "test-merch"   → merch product page (safe testing)
# "live-tickets" → live ticket sale page
MONITOR_MODE=test-merch

# ── Target URL ────────────────────────────────────────────────────────────────
# Leave blank to use the default for the selected mode.
# test-merch default:  https://shop.royalchallengers.com/merchandise/152
# live-tickets default: https://shop.royalchallengers.com/
TARGET_URL=

# ── Polling ───────────────────────────────────────────────────────────────────
CHECK_INTERVAL=120

# ── Price filter (live-tickets mode) ──────────────────────────────────────────
MIN_PRICE=2000
MAX_PRICE=5000

# ── Checkout details ─────────────────────────────────────────────────────────
QUANTITY=4
MIN_QUANTITY=2
NAMES=Your Name
UPI_VPA=yourvpa@upi
# Merch product options (leave blank to skip/use default)
MERCH_SIZE=L
MERCH_CATEGORY=

# ── Contact (used if login/guest forms appear) ───────────────────────────────
CONTACT_EMAIL=you@example.com
CONTACT_PHONE=9999999999
GENDER=Male

# ── Shipping Address ─────────────────────────────────────────────────────────
ADDRESS_LINE1=Your Address Line 1
ADDRESS_LINE2=Your Address Line 2
ADDRESS_LANDMARK=Nearby Landmark
ADDRESS_CITY=Your City
ADDRESS_STATE=Your State
ADDRESS_PINCODE=000000

# ── Notifications & alerts ────────────────────────────────────────────────────
ENABLE_NOTIFICATIONS=1
# Gmail SMTP for auto-sending email (optional — falls back to Gmail compose draft)
# To get app password: Google Account → Security → 2-Step Verification → App passwords
SMTP_EMAIL=
SMTP_PASSWORD=
# Recipient email
NOTIFY_EMAIL=
# WhatsApp numbers (comma-separated, with country code, no + prefix)
NOTIFY_WHATSAPP=

# ── Misc ──────────────────────────────────────────────────────────────────────
MAX_RETRIES=5
SCREENSHOT_DIR=screenshots
# Keep screenshots from the last N check cycles (older ones auto-deleted)
SCREENSHOT_KEEP_CYCLES=4
LOG_FILE=monitor.log
# Seconds to wait for manual seat selection if auto-select fails on venue map
MANUAL_SEAT_TIMEOUT=60
# Seconds to wait for user to enter OTP during login
OTP_TIMEOUT=90
# Chrome profile dir for session persistence (login survives restarts)
# Delete this folder to force a fresh login
CHROME_PROFILE_DIR=chrome_profile

# ── Scrapling (optional stealth browser) ──────────────────────────────────────
# Use Scrapling/Camoufox stealth browser for parallel workers (live-tickets mode).
# Workers 1-6 use anti-detection Camoufox instead of raw Chrome. Worker 0 stays Selenium.
# Requires: pip install scrapling[camoufox]
USE_STEALTH_BROWSER=0
